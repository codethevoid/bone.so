import { Showcase } from "../../../components/showcase";
import { Button } from "@bonekit/ui/button";
import { CodeBlock } from "../../../components/code-block";
import { CodeSnippet } from "../../../components/code-snippet";
import { Table } from "../../../components/table";
import { Terminal } from "../../../components/terminal";
import { Tab, TabIndicator, TabPanel, Tabs, TabsList } from "../../../components/tabs";
import { custom, danger, example, ghost, link, manual, outline, primary, soft } from "../../code-examples/button";

export const metadata = {
  title: "Button",
  description: "Displays a button than can be clicked.",
};

<Showcase>
  <Button>Click me</Button>
</Showcase>

<CodeBlock filename={"btn-example.tsx"} code={example} />

## Installation

<Tabs defaultValue="global">
  <TabsList className="relative">
    <Tab value="global">Global</Tab>
    <Tab value="manual">Manual</Tab>
    <TabIndicator />
  </TabsList>
  <TabPanel value="global">
    <Terminal
      commands={[
        { label: "pnpm", command: "pnpm add @bonekit/ui" },
        { label: "npm", command: "npm install @bonekit/ui" },
        { label: "yarn", command: "yarn add @bonekit/ui" },
        { label: "bun", command: "bun add @bonekit/ui" },
      ]}
    />
  </TabPanel>
  <TabPanel value="manual">
    <div className="mt-4">
      Install the following dependencies:
      <Terminal className="mb-12"
                commands={[
                  {
                    label: "pnpm",
                    command: "pnpm add @radix-ui/react-slot class-variance-authority",
                  },
                  {
                    label: "npm",
                    command: "npm install radix-ui/react-slot class-variance-authority",
                  },
                  {
                    label: "yarn",
                    command: "yarn add @radix-ui/react-slot class-variance-authority",
                  },
                  {
                    label: "bun",
                    command: "bun add @radix-ui/react-slot class-variance-authority",
                  },
                ]}
      />

      Copy and paste the following code into your button component:

      <CodeBlock expandable className="mt-6" withShowcase={false} filename="button.tsx"
                 code={manual} />
    </div>

  </TabPanel>
</Tabs>

## Usage

<CodeSnippet
  filename={"index.tsx"}
  code='import { Button } from "@bonekit/ui/button";'
/>
<CodeSnippet filename="index.tsx" code="<Button>Click me</Button>" />

## Examples

The following variants are available for the Button component:

### Primary

Primary buttons are the default style and are used for main actions.

<Showcase>
  <Button>Primary</Button>
</Showcase>

<CodeBlock filename={"primary-btn.tsx"} code={primary} />

### Soft

Soft buttons have a subtle background and are used for less prominent actions.

<Showcase>
  <Button variant="soft">Soft</Button>
</Showcase>

<CodeBlock filename={"soft-btn.tsx"} code={soft} />

### Outline

Outline buttons have a border and are used for secondary actions.

<Showcase>
  <Button variant="outline">Outline</Button>
</Showcase>

<CodeBlock filename={"outline-btn.tsx"} code={outline} />

### Ghost

Ghost buttons have no background or border and are used for minimalistic designs.

<Showcase>
  <Button variant="ghost">Ghost</Button>
</Showcase>

<CodeBlock filename={"ghost-btn.tsx"} code={ghost} />

### Danger

Danger buttons are used for destructive actions and have a red color scheme.

<Showcase>
  <Button variant="danger">Danger</Button>
</Showcase>

<CodeBlock filename={"danger-btn.tsx"} code={danger} />

### Custom

You can apply custom styles using Tailwind CSS classes.

<Showcase>
  <Button className="border-0 bg-gradient-to-tr from-blue-400 to-blue-600 text-white shadow-xl shadow-blue-600/20 hover:scale-[103%] hover:opacity-100 focus:ring-0 active:scale-[98%] dark:shadow-blue-500/20">
    <span>Custom</span>
  </Button>
</Showcase>

<CodeBlock filename="custom-btn.tsx" code={custom} />

### Link

You can make any element look like the button component by using the `passThrough` prop. This is useful for creating links that look like buttons.

<Showcase>
  <Button passThrough>
    <a
      href="https://github.com/codethevoid/bone.so"
      target="_blank"
      rel="noopener noreferrer"
    >
      <span>View Github</span>
    </a>
  </Button>
</Showcase>

<CodeBlock filename="link-btn.tsx" code={link} />

## Props

The Button component accepts the following props:

<Table
  head={["Prop", "Type", "Default"]}
  rows={[
    ["passThrough", "boolean", "false"],
    ["variant", "primary | soft | outline | ghost | danger", "primary"],
    ["size", "tiny | small | default | large", "default"],
    ["className", "string", "undefined"],
  ]}
/>
