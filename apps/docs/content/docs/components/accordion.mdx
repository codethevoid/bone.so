import {Showcase} from "../../../components/showcase";
import {Accordion, AccordionItem, AccordionPanel, AccordionTrigger} from "@bonekit/ui/accordion";
import {CodeBlock} from "../../../components/code-block";
import {Button} from "@bonekit/ui/button";
import {ArrowUpRight} from "lucide-react";

export const metadata = {
    title: 'Accordion',
    description: 'A set of collapsible panels with headings.',
}

<Button size="tiny" variant="soft" className="mt-4">Api reference<ArrowUpRight className="size-3.5"/></Button>

<Showcase>
    <Accordion className="w-full max-w-96" openMultiple={false}>
        <AccordionItem>
            <AccordionTrigger>Minimal by default</AccordionTrigger>
            <AccordionPanel>
                <div className="-mt-2">
                    Bone UI ships with sleek, accessible components that are lightweight and production ready but built to be styled and extended to match your design system.
                </div>
            </AccordionPanel>
        </AccordionItem>
        <AccordionItem>
            <AccordionTrigger>Powered by Base UI</AccordionTrigger>
            <AccordionPanel>
                <div className="-mt-2">
                    Built on top of Base UI (from the creators of MUI and Radix), Bone UI layers modern design on top of a headless foundation.
                </div>
            </AccordionPanel>
        </AccordionItem>
        <AccordionItem>
            <AccordionTrigger>What is Bone UI please tell me so?</AccordionTrigger>
            <AccordionPanel>
                <div className="-mt-2">
                    A set of collapsible panels with headings.
                </div>
            </AccordionPanel>
        </AccordionItem>
    </Accordion>
</Showcase>

<CodeBlock filename="accordion-example.tsx" expandable
           code={`import { Accordion, AccordionItem, AccordionPanel, AccordionTrigger } from '@bonekit/ui/accordion';

const AccordionExample = () => {
    return (
      <Accordion className="w-full max-w-96" openMultiple={false}>
        <AccordionItem>
          <AccordionTrigger>What is Bone UI?</AccordionTrigger>
        </AccordionItem>
      </Accordion>
    );
}
`}/>